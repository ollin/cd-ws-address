withEnv(["PIPE_FLOW_NUMBER=${currentBuild.number}"]) {
  node {
    def workspace = pwd()
    if(isUnix()){
      sh "${workspace}@script/gradlew pushImages --info"

    }
    else{
      bat "${workspace}@script/gradlew.bat pushImages --info"
    }
  }
}
